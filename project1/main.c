#include <stdio.h>
#include <stdlib.h>

int main()
{
    int Array();
for (x = 0; x < 6; x++)
    {
    for (y = 0; y < 6; y++)
    {
        for (z = 0; z < 6; z++)
        {
            [x][y][z] = x * y * z;
        }
    }
    }
getch();
}
